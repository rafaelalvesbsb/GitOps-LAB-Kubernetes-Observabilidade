apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: monitoring-local-dev-tls
  namespace: monitoring
spec:
  secretName: monitoring-local-dev-tls
  dnsNames:
    - grafana.local.dev
    - prometheus.local.dev
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana
  namespace: monitoring
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  tls:
    - hosts: [grafana.local.dev]
      secretName: monitoring-local-dev-tls
  rules:
    - host: grafana.local.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monitoring-dev-grafana
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prometheus
  namespace: monitoring
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  tls:
    - hosts: [prometheus.local.dev]
      secretName: monitoring-local-dev-tls
  rules:
    - host: prometheus.local.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: monitoring-dev-kube-promet-prometheus
                port:
                  number: 9090
