apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: dashboard-local-dev-tls
  namespace: dashboard
spec:
  secretName: dashboard-local-dev-tls
  dnsNames:
    - dashboard.local.dev
  issuerRef:
    name: selfsigned-issuer
    kind: ClusterIssuer
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: headlamp
  namespace: dashboard
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: "HTTP"
spec:
  ingressClassName: nginx
  tls:
    - hosts: [dashboard.local.dev]
      secretName: dashboard-local-dev-tls
  rules:
    - host: dashboard.local.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: headlamp
                port:
                  number: 80
